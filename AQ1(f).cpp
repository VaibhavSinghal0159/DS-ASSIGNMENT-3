#include <bits/stdc++.h>
using namespace std;

int main() {
    int size = 5;
    int top = 4; 
    if (top == size - 1) {
        cout << "Stack is full";
    } else {
        cout << "Stack is not full";
    }

    return 0;
}
